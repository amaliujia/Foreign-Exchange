Source files:

RandomForest.java ---- RandomRorest implementation
RealDecisionTree.java ---- Decision Tree that work on contiguous attributes.

More explaination: Basically, Random Forest has lots of trees, it trains these trees independently, and when training each tree, the attributes(features) are a rondomly
selected subset of all attributes. 