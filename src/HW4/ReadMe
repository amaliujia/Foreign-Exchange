Source Code
---------------------------

Label.java. Label class reads finance data in CSV format, extracts features from data, and writes feature matrix into Cassandra.

RandomForest.java. RandomForest class reads training and testing data from Cassandra table, builds Forest, and tests it.

RealDecisionTree.java. RealDecisionTree class is a tree working with real number attributes.

CassandraDriver.java. CassandraDriver class provides application API to learning algorithms that help insert and query data which saves in Cassandra tables.